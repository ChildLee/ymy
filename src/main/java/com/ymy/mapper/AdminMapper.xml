<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ymy.mapper.AdminMapper">

    <resultMap id="adminBean" type="Admin">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="level" jdbcType="BIGINT" property="level"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="last_login_time" jdbcType="TIMESTAMP" property="lastLoginTime"/>
        <result column="status" jdbcType="TINYINT" property="status"/>
    </resultMap>

    <insert id="addAdmin">
        INSERT INTO admin VALUES(NULL,#{username},#{password},#{level},DEFAULT,DEFAULT,#{status})
    </insert>

    <update id="updateAdmin">
        UPDATE admin SET password=#{password},
        create_time=#{createTime},
        status=#{status}
        WHERE id=#{id}
    </update>

    <delete id="deleteAdmin">
        DELETE FROM admin where id=#{id}
    </delete>

    <select id="getAdmin" resultMap="adminBean">
        select * from admin
    </select>
</mapper>